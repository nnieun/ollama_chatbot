# React 프론트엔드 개발 규칙 — 3-File System

## 작업 범위

- 이 프로젝트의 React 프론트엔드는 3-File System 방식으로 개발한다.
- 프론트엔드 React 코드만 변경할 수 있다.
- 백엔드 코드, 데이터베이스, 인프라, 배포 설정은 변경하지 않는다.
- 기존 API URL, HTTP 메서드, 요청 필드, 응답 필드, 인증 방식을 변경하지 않는다.
- 프론트엔드 설정 파일은 프론트엔드 구현에 꼭 필요한 경우에만 변경한다.

## UI 기준

- 프로젝트 안의 `docs/ui설계도.png`를 화면 설계 기준으로 사용한다.
- 이미지에서 직접 확인되는 내용과 추론한 내용을 구분한다.
- 이미지에서 확인할 수 없는 인터랙션, 반응형 동작, 오류 상태는 임의로 결정하지 않고 질문한다.
- 이미지에 없는 기능을 임의로 추가하지 않는다.
- 기존 디자인 시스템이 있다면 우선 재사용한다.

## Context7 규칙

- React 또는 외부 라이브러리의 API, 설정, 사용법이 필요한 경우 항상 Context7을 사용한다.
- 먼저 `package.json`과 lock 파일에서 실제 설치 버전을 확인한다.
- Context7에서 정확한 라이브러리 ID를 확인한 후 관련 문서를 조회한다.
- 프로젝트 버전과 호환되지 않는 최신 API를 임의로 사용하지 않는다.
- Context7에서 확인할 수 없는 API는 사용하지 말고 확인 불가로 보고한다.

## 작업 통제

- PRD 승인 전에는 구현하지 않는다.
- 작업 목록 승인 전에는 구현하지 않는다.
- 한 번에 하나의 하위 작업만 수행한다.
- 승인받지 않은 리팩터링이나 패키지 설치를 하지 않는다.
- 패키지 설치가 필요하면 목적, 버전, 대안, 영향 범위를 먼저 설명하고 승인을 기다린다.
- 관련 없는 기존 코드를 수정하지 않는다.
- 작업 완료 후 실제 실행한 검증 결과를 보고한다.
- 실행하지 않은 검사를 통과했다고 표현하지 않는다.
- 실패한 테스트가 있으면 작업을 완료 처리하지 않는다.

## 언어 및 파일 규칙

- TypeScript를 사용하지 않는다.
- 모든 프론트엔드 코드는 JavaScript로 작성한다.
- React 컴포넌트는 프로젝트 기존 규칙에 따라 `.jsx` 또는 `.js`를 사용한다.
- `.ts` 및 `.tsx` 파일을 생성하지 않는다.
- TypeScript, `@types/*`, `tsconfig.json`을 새로 추가하지 않는다.
- TypeScript 전환이나 도입을 제안하지 않는다.
- 타입 검사는 TypeScript 대신 기존 ESLint, 테스트, 런타임 검증으로 수행한다.
- 프로젝트에 이미 TypeScript 파일이 있더라도 이번 작업에서는 수정하지 않는다.
- 라이브러리 예제는 Context7에서 확인하되 JavaScript 사용 예제를 우선 적용한다.
- Context7 문서가 TypeScript 예제만 제공하면 타입 문법을 단순 제거하지 말고, 동일 API의 JavaScript 사용 가능 여부를 문서에서 확인한 후 적용한다.
