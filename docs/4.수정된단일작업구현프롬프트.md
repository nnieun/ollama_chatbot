task-list.mdc와 tasks/tasks-prd-[feature-name].md를 읽고,
첫 번째 미완료 하위 작업 하나만 수행해줘.

[언어 제약]
- React + JavaScript만 사용한다.
- 프로젝트 기존 규칙에 따라 .jsx 또는 .js를 사용한다.
- .ts 및 .tsx 파일을 생성하거나 수정하지 않는다.
- TypeScript 문법, 설정, 패키지를 추가하지 않는다.
- JSDoc은 기존 프로젝트에서 사용 중인 경우에만 동일한 방식으로 작성한다.

[구현 제약]
- 승인된 작업 범위만 변경한다.
- 백엔드와 API 계약을 변경하지 않는다.
- 승인되지 않은 패키지를 설치하지 않는다.
- ui설계.jpg에 없는 기능을 임의로 추가하지 않는다.
- 기존 컴포넌트와 프로젝트 패턴을 우선 재사용한다.
- React 및 외부 라이브러리는 Context7에서 확인한 JavaScript 사용법을 따른다.
- Context7의 TypeScript 예제를 근거 없이 JavaScript로 변환하지 않는다.
- 관련 없는 리팩터링을 하지 않는다.

[검증]
- ESLint
- 관련 테스트
- 기존 전체 프론트엔드 테스트
- 프로덕션 빌드
- 브라우저 런타임 오류 가능성
- 백엔드 파일 무변경
- .ts 및 .tsx 파일 무생성
- package.json의 승인되지 않은 변경 여부

package.json에 존재하는 명령만 실행한다.
실행하지 않은 검사를 통과했다고 보고하지 않는다.
실패한 검사가 있으면 작업을 [x]로 표시하지 않는다.

완료 후 다음 내용을 보고해:
1. 변경 파일
2. 구현 내용
3. Context7에서 확인한 문서와 적용 사항
4. 실행 명령과 실제 결과
5. 인수 조건 충족 여부
6. TypeScript 파일을 생성하거나 수정하지 않았는지
7. 백엔드 변경 여부
8. 남은 위험

다음 작업은 시작하지 마.

use context7